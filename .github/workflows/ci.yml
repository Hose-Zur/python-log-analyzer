# name: CI (python-log-analyzer)
# on:
#   pull_request:
#   push:
#     branches: [ main ]
#
# jobs:
#   build-test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'   # TODO: dopasuj do pyproject (zalecane ^3.11)
#
#       - name: Install deps
#         run: |
#           pipx install poetry
#           poetry install --no-root
#
#       - name: Lint (ruff)
#         run: |
#           poetry run ruff check .
#
#       - name: Format check (black)
#         run: |
#           poetry run black --check .
#
#       - name: Tests (pytest)
#         run: |
#           poetry run pytest -q
#
#       # (opcjonalnie) Pokrycie
#       # - name: Coverage
#       #   run: poetry run pytest -q --cov=src --cov-report=term-missing
#
#       # (opcjonalnie) Artefakty raportów (gdy reporter powstanie)
#       # - name: Upload reports
#       #   uses: actions/upload-artifact@v4
#       #   with:
#       #     name: sample-reports
#       #     path: reports/
